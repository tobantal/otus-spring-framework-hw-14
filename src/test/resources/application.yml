spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: pas
    data: classpath:/data-test.sql
  shell:
    interactive:
      enabled:  false
